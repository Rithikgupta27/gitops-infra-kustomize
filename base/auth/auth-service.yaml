apiVersion: v1
kind: Service 
metadata:
  name: auth-service             
  namespace: microservice
spec:
  selector:
    app: auth-microservice                
  ports:
    - protocol: TCP
      port: 80               
      targetPort: 3000       
  type: ClusterIP

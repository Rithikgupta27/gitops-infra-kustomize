apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../../base/auth

labels:
  - pairs: 
     env: dev 
    includeSelectors: false
    includeTemplates: true

configMapGenerator:
  - name: dev-envs
    envs:
      - dev.properties

patches:
  - path: auth-deployment-patch.yaml
# generators:
#   - auth-secrets-generator.yaml